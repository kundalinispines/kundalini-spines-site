One-line: the instrument block — mono keys at gray-300, values in bone, the site's factual texture.

    <ChakraReadout head="CALIBRATION" />
    <ChakraReadout head="RECORD" rows={[['TRACKS','28'],['RUNTIME','1:50:55']]} />

gray-300 is the FLOOR for the keys, not a preference: gray-400 measures 3.94:1 and cannot reach AA on this site's backdrop. Never dim the block with a group opacity — over the lit sky it inverts and the labels dissolve while the numbers survive.
