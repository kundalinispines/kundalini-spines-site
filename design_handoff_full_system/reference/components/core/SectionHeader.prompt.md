One-line: the standard section opening — mono eyebrow, condensed display headline, optional one-paragraph lede.

```jsx
<SectionHeader eyebrow="The Messengers" title="Two Messengers. One Signal." description="The Messengers don't claim mastery over the Signal." />
```

Set `stencil` only where the type is large — the homepage's "Enter the Tracks" and the Transmissions H1 use it.
