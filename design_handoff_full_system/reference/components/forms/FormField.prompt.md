One-line: the only text input in the system — surface-grey box, hairline border, mono uppercase label above it.

    <FormField id="email" label="Email address" type="email" placeholder="you@email.com" />

Labels are always present in the DOM. Errors are sentences, not codes.
