One-line: the minimal play control under the focused track — ring, glyph and a 3px bar, all in the track's sampled accent.

    <SamplePlayer playing progress={42} />

Only one sample plays anywhere on the page at a time. The ring AND the glyph take the accent, so the button reads as part of the artwork rather than outlined in it.
