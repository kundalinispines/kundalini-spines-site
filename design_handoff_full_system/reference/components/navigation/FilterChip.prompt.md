One-line: the Archive filter control — a row of square mono chips, one active.

    <FilterChip active onClick={fn}>All</FilterChip>
    <FilterChip onClick={fn}>Artwork</FilterChip>

Always keep an "All" chip first and always leave one chip active. Categories come from data/archive.json.
