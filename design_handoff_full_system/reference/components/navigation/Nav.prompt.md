One-line: the site header — spine mark + wordmark left, five uppercase links right.

    <Nav items={[{label:'Home',href:'/'},{label:'Music',href:'/#tracks'}]} current="/" scrolled />

The scrolled bar is OPAQUE black, not 92% + blur: at the #tracks landing position the hero's white CTA button sits behind it and 8% of white reads as a lit rectangle. Every page except the homepage renders in the scrolled state from the start.
