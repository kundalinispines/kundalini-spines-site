One-line: one vertebra endpoint on the navigator — an amber light point that brightens, throws a crosshair and opens a card.

    <SpineNode label="Transmissions" y="40%" side="right" state="hover" />
    <SpineNode label="Music" kind="immersive" y="66%" side="left" state="active" />

Amber (228,232,235) belongs to the nodes and nothing else — it is what distinguishes a destination from the white spine. When one node is active the others recede to 0.78, not to nothing: the spine stays legible as a whole.
