One-line: the Transmissions page rendered as a piece of hardware — each platform is a frequency you tune to.

    <Terminal status="Receiving" channels={<ChannelTab selected>All</ChannelTab>}><TerminalRow ... /></Terminal>

The scanline overlay is 2.2% white at 1px/3px and 50% opacity — barely there. The live dot pulses at 2.4s; the caret blinks in 2 steps at 1.1s. Both stop under prefers-reduced-motion.
